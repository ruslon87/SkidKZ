import 'package:flutter/material.dart';

class BuyerFavoritesScreen extends StatelessWidget {
  const BuyerFavoritesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Избранное (заглушка)'),
    );
  }
}
