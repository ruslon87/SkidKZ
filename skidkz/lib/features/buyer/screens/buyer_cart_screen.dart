import 'package:flutter/material.dart';

class BuyerCartScreen extends StatelessWidget {
  const BuyerCartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Корзина (заглушка)'),
    );
  }
}
